#!/usr/bin/python
# -*- coding: utf-8 -*-
from COMMON import *

def CANDIDATE_ELIMINATION():
  version_space = ALL_VERSION_SPACE()
  #line_print(version_space)
  #print(len(version_space));
  hx = []
  for v in version_space:
    for entry in e:
      if entry.keys()[0] == "Yes":
        value = entry["Yes"]

if __name__ == "__main__":
  print("CANDIDATE-ELIMINAION")
  CANDIDATE_ELIMINATION()
